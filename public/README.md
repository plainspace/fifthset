# Public Assets

## noise.png
A subtle grain/noise texture for the speakeasy overlay effect.
Generate a 200x200 noise texture PNG with very subtle monochrome grain.
Used in globals.css .grain::after with opacity: 0.03.
